/**
 * Trending Now content is dynamic (live `/trending` API).
 * Tests discover visible chip text at runtime — no fixed label catalog.
 */
export const trendingNowQueries = {
  /** Placeholder for docs / future deterministic fixtures. */
  discoverAtRuntime: true,
} as const;
